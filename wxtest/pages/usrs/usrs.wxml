<!--pages/usrs/usrs.wxml-->
<view class="container log-list">
<block wx:for="{{userList}}"  wx:key="{{item.usr_id}}">
    <text  class="log-item">{{index+1}}——{{item.usr_name}}</text>
</block>
</view>
